# Frontend Environment Variables
# These are used by the Vite/React frontend

# API Configuration
# For local development, use the backend server directly
VITE_API_URL=http://localhost:3001/api

# Note: Database and other secrets are configured in Vercel dashboard
# DO NOT commit sensitive credentials to git
