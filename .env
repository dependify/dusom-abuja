# Frontend Environment Variables
# These are used by the Vite/React frontend

# API Configuration
# For local development:
# VITE_API_URL=http://localhost:3001/api
# For production (Vercel) - API calls go to /api which is proxied to backend:
VITE_API_URL=/api

# Supabase (Legacy - removed)
# No longer used - migrated to custom PostgreSQL backend
